# Web Builder — Claude Code Fallback

For website/web-app implementation tasks, use the canonical Web Blueprint Engine skill at `skill/SKILL.md`.

The skill is direct-build: ask only missing discovery questions, inspect the repository, implement real code, run verification, and fix failures. Do not replace implementation with PRD/TRD/prompt documents unless the user explicitly requests documentation.
