# Web Builder

Use `skills/web-builder/SKILL.md` as the canonical workflow for website and web-app tasks. Ask only missing discovery questions and then edit the repository directly. Consult `references/` when useful. Test and verify before claiming completion.