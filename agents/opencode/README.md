# OpenCode Adapter

OpenCode supports project skill discovery and also supports plugin configuration. Use `.opencode/skills/web-builder/SKILL.md` for the native skill path; `skills/web-builder/SKILL.md` remains the canonical portable copy.

Use the shared references rather than duplicating implementation policy.