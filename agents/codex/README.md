# Codex Adapter

Codex can use the shared Agent Skills layout. The repository ships the skill at `skills/web-builder/SKILL.md` and a discovery copy at `.agents/skills/web-builder/SKILL.md`.

The root `AGENTS.md` provides an instruction-tier fallback for Codex environments that load repository guidance automatically.