# Cursor Adapter

Cursor uses the repository rule adapter at `.cursor/rules/web-builder.mdc`. It points the agent to `skills/web-builder/SKILL.md` and the shared references. No separate implementation logic is maintained.