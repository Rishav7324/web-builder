# Web Builder Agent Adapter

Load `skills/web-builder/SKILL.md` for the full behavior.

This host adapter exists only to bridge the host's discovery mechanism to the shared skill. Do not fork or rewrite the core instructions here.

When the user asks to build or redesign a website/web app: load the skill, ask only missing discovery questions, then implement real code directly. Use `references/` as needed and verify the result.