# Claude Code Adapter

Native Claude Code integration uses `.claude-plugin/plugin.json` and `.claude/skills/web-builder/SKILL.md`. The canonical skill is shared; this adapter contains no divergent behavior.

Install from the repository using Claude Code's plugin/skill installation flow, or copy `.claude/skills/web-builder` into a project.
