# Testing Reference

After implementation run the project's native lint, typecheck, tests, and production build when available. If browser automation exists, verify the public entry, primary CTA, auth, core product action, navigation, mobile layout, and meaningful error/empty states.

Fix deterministic failures and re-run checks. Never describe a path as fully functional when a required dependency or credential is knowingly missing.